spring.application.name=question-service
server.port=${PORT:8080}

# Neon PostgreSQL connection
#spring.datasource.url=jdbc:postgresql://ep-summer-resonance-a108hckj-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
#spring.datasource.username=neondb_owner
#spring.datasource.password=npg_E5TYFyAxU6kg

spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Eureka client settings (to register with your Eureka server on Render)
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=https://server-registry-eureka-server.onrender.com/eureka/
